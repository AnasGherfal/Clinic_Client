// Import required modules

import Layout from '../../layout';
import Timeline from './TimeLine';

// Define the generateTimeslots function


  const Appointment = () => {
  
    
  
    return (
      <Layout>
        <Timeline/>
      {/* <Timeline events={events} /> */}
      </Layout>
    );
  };

// Export the Appointment component
export default Appointment;